services:
  - type: web
    name: royals-webtech-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --timeout 120 app:app
    healthCheckPath: /health
